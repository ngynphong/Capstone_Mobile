import { useNotificationContext } from '../context/NotificationContext';

// Re-export the context hook as useNotifications for backward compatibility
export const useNotifications = useNotificationContext;
